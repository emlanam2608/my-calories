import { Check, LoaderCircle } from 'lucide-react';
import { useState, type ReactNode } from 'react';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import type { OnboardingDraft } from '@/lib/contracts';
import { getCopy, type Locale } from '@/lib/copy';

type ChoiceField = 'foodPreferences' | 'allergies' | 'reportedContexts' | 'injuryFlags' | 'symptomFlags' | 'availableDays' | 'equipment' | 'environments' | 'clinicianRestrictionFlags' | 'medicationExerciseRiskFlags';

function SelectField({ label, value, onChange, required, children }: { label: string; value: string; onChange: (value: string) => void; required?: boolean; children: ReactNode }) { return <label className="text-sm font-semibold text-slate-800">{label}<select value={value} required={required} onChange={(event) => onChange(event.target.value)} className="mt-2 flex h-10 w-full rounded-md border border-input bg-white px-3 text-sm">{children}</select></label>; }
function NumberField({ label, value, min, max, step = '1', required, onChange }: { label: string; value: number | undefined; min: string; max: string; step?: string; required?: boolean; onChange: (value: string) => void }) { return <label className="text-sm font-semibold text-slate-800">{label}<Input className="mt-2 bg-white" type="number" min={min} max={max} step={step} required={required} value={value ?? ''} onChange={(event) => onChange(event.target.value)} /></label>; }

export function OnboardingPanel({ initialDraft, status, saving, onSave, locale }: { initialDraft: OnboardingDraft; status: 'in_progress' | 'complete'; saving: boolean; onSave: (draft: OnboardingDraft) => void; locale: Locale }) {
  const c = getCopy(locale); const [draft, setDraft] = useState(initialDraft);
  const setNumber = (field: 'heightCm' | 'weightKg' | 'ageYears' | 'sleepHours', value: string) => { const number = Number(value); setDraft((current) => ({ ...current, [field]: value === '' || !Number.isFinite(number) ? undefined : number })); };
  const toggle = (field: ChoiceField, value: string) => setDraft((current) => { const values = (current[field] ?? []) as string[]; if (field === 'symptomFlags') { if (value === 'none') return { ...current, symptomFlags: values.includes('none') ? [] : ['none'] } as OnboardingDraft; const withoutNone = values.filter((item) => item !== 'none'); return { ...current, symptomFlags: withoutNone.includes(value) ? withoutNone.filter((item) => item !== value) : [...withoutNone, value] } as OnboardingDraft; } return { ...current, [field]: values.includes(value) ? values.filter((item) => item !== value) : [...values, value] } as OnboardingDraft; });
  const sets: Array<{ field: ChoiceField; label: string; options: Array<[string, string]> }> = [
    { field: 'foodPreferences', label: c.onboarding.preferences, options: [['omnivore', c.onboarding.preferenceOmnivore], ['vegetarian', c.onboarding.preferenceVegetarian], ['vegan', c.onboarding.preferenceVegan], ['pescatarian', c.onboarding.preferencePescatarian], ['halal', c.onboarding.preferenceHalal], ['low_sodium', c.onboarding.preferenceLowSodium], ['low_purine', c.onboarding.preferenceLowPurine]] },
    { field: 'allergies', label: c.onboarding.allergies, options: [['milk', c.onboarding.allergyMilk], ['egg', c.onboarding.allergyEgg], ['fish', c.onboarding.allergyFish], ['shellfish', c.onboarding.allergyShellfish], ['peanut', c.onboarding.allergyPeanut], ['tree_nut', c.onboarding.allergyTreeNut], ['soy', c.onboarding.allergySoy], ['wheat', c.onboarding.allergyWheat], ['sesame', c.onboarding.allergySesame]] },
    { field: 'reportedContexts', label: c.onboarding.contexts, options: [['blood_pressure', c.onboarding.contextBloodPressure], ['cholesterol', c.onboarding.contextCholesterol], ['blood_glucose', c.onboarding.contextBloodGlucose], ['uric_acid', c.onboarding.contextUricAcid]] },
    { field: 'injuryFlags', label: c.onboarding.injuries, options: [['back_pain', c.onboarding.injuryBack], ['joint_pain', c.onboarding.injuryJoint], ['balance_concern', c.onboarding.injuryBalance]] },
    { field: 'symptomFlags', label: c.onboarding.symptoms, options: [['none', c.onboarding.symptomNone], ['chest_discomfort', c.onboarding.symptomChest], ['dizziness', c.onboarding.symptomDizziness], ['shortness_of_breath', c.onboarding.symptomBreath]] },
    { field: 'availableDays', label: c.onboarding.days, options: [['mon', c.onboarding.dayMon], ['tue', c.onboarding.dayTue], ['wed', c.onboarding.dayWed], ['thu', c.onboarding.dayThu], ['fri', c.onboarding.dayFri], ['sat', c.onboarding.daySat], ['sun', c.onboarding.daySun]] },
    { field: 'equipment', label: c.onboarding.equipment, options: [['bodyweight', c.onboarding.equipmentBodyweight], ['chair', c.onboarding.equipmentChair], ['exercise_mat', c.onboarding.equipmentMat], ['bicycle', c.onboarding.equipmentBicycle], ['mini_treadmill', c.onboarding.equipmentTreadmill], ['resistance_band', c.onboarding.equipmentBand], ['dumbbells', c.onboarding.equipmentDumbbells], ['gym', c.onboarding.equipmentGym]] },
    { field: 'environments', label: c.onboarding.environments, options: [['home', c.onboarding.environmentHome], ['outdoors', c.onboarding.environmentOutdoors], ['gym', c.onboarding.environmentGym]] },
    { field: 'clinicianRestrictionFlags', label: c.onboarding.restrictions, options: [['avoid_high_intensity', c.onboarding.restrictionIntensity], ['avoid_resistance', c.onboarding.restrictionResistance], ['avoid_impact', c.onboarding.restrictionImpact], ['monitor_glucose', c.onboarding.restrictionGlucose]] },
    { field: 'medicationExerciseRiskFlags', label: c.onboarding.medicationRisks, options: [['glucose_lowering_without_plan', c.onboarding.medicationGlucose], ['dizziness_or_fainting_risk', c.onboarding.medicationDizziness], ['other_exercise_restriction', c.onboarding.medicationOther]] },
  ];
  return <Card className="mt-7 border-none shadow-sm"><CardHeader><p className="text-xs font-bold uppercase tracking-[0.15em] text-emerald-700">{c.onboarding.eyebrow}</p><CardTitle className="flex flex-wrap items-center gap-2">{c.onboarding.title}<span className={`rounded-full px-2 py-0.5 text-xs font-medium ${status === 'complete' ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-900'}`}>{status === 'complete' ? c.onboarding.complete : c.onboarding.inProgress}</span></CardTitle><CardDescription>{c.onboarding.description}</CardDescription></CardHeader><CardContent><form onSubmit={(event) => { event.preventDefault(); onSave(draft); }} className="space-y-5"><div className="grid gap-4 sm:grid-cols-2"><SelectField label={c.onboarding.goal} value={draft.goal ?? ''} onChange={(value) => setDraft((current) => ({ ...current, goal: value === '' ? undefined : value as OnboardingDraft['goal'] }))} required><option value="">{c.common.required}</option><option value="weight_loss">{c.onboarding.goalWeightLoss}</option><option value="maintain_weight">{c.onboarding.goalMaintain}</option><option value="muscle_gain">{c.onboarding.goalMuscleGain}</option><option value="fitness">{c.onboarding.goalFitness}</option><option value="health_tracking">{c.onboarding.goalHealthTracking}</option></SelectField><SelectField label={c.onboarding.sex} value={draft.sexForMetabolicCalculation ?? ''} onChange={(value) => setDraft((current) => ({ ...current, sexForMetabolicCalculation: value === '' ? undefined : value as OnboardingDraft['sexForMetabolicCalculation'] }))} required><option value="">{c.common.required}</option><option value="female">{c.onboarding.sexFemale}</option><option value="male">{c.onboarding.sexMale}</option><option value="not_specified">{c.onboarding.sexNotSpecified}</option></SelectField><NumberField label={c.onboarding.height} value={draft.heightCm} min="80" max="250" required onChange={(value) => setNumber('heightCm', value)} /><NumberField label={c.onboarding.weight} value={draft.weightKg} min="1" max="500" onChange={(value) => setNumber('weightKg', value)} /><NumberField label={c.onboarding.age} value={draft.ageYears} min="1" max="120" required onChange={(value) => setNumber('ageYears', value)} /><NumberField label={c.onboarding.sleep} value={draft.sleepHours} min="0" max="24" step="0.5" onChange={(value) => setNumber('sleepHours', value)} /><SelectField label={c.onboarding.activity} value={draft.activityLevel ?? ''} onChange={(value) => setDraft((current) => ({ ...current, activityLevel: value === '' ? undefined : value as OnboardingDraft['activityLevel'] }))} required><option value="">{c.common.required}</option><option value="sedentary">{c.onboarding.activitySedentary}</option><option value="light">{c.onboarding.activityLight}</option><option value="moderate">{c.onboarding.activityModerate}</option><option value="active">{c.onboarding.activityActive}</option><option value="very_active">{c.onboarding.activityVeryActive}</option></SelectField><SelectField label={c.onboarding.training} value={draft.trainingHistory ?? ''} onChange={(value) => setDraft((current) => ({ ...current, trainingHistory: value === '' ? undefined : value as OnboardingDraft['trainingHistory'] }))} required><option value="">{c.common.required}</option><option value="new_to_exercise">{c.onboarding.trainingNew}</option><option value="beginner">{c.onboarding.trainingBeginner}</option><option value="regular">{c.onboarding.trainingRegular}</option></SelectField><SelectField label={c.onboarding.pregnancyContext} value={draft.pregnancyContext ?? ''} onChange={(value) => setDraft((current) => ({ ...current, pregnancyContext: value === '' ? undefined : value as OnboardingDraft['pregnancyContext'] }))}><option value="">{c.onboarding.pregnancyNotRecorded}</option><option value="not_applicable">{c.onboarding.pregnancyNotApplicable}</option><option value="not_pregnant">{c.onboarding.pregnancyNotPregnant}</option><option value="pregnant">{c.onboarding.pregnancyPregnant}</option><option value="postpartum">{c.onboarding.pregnancyPostpartum}</option><option value="unsure">{c.onboarding.pregnancyUnsure}</option><option value="prefer_not_to_say">{c.onboarding.pregnancyPreferNot}</option></SelectField></div>{sets.map(({ field, label, options }) => { const selected = (draft[field] ?? []) as string[]; return <fieldset key={field}><legend className="mb-2 text-sm font-semibold text-slate-800">{label}{['availableDays', 'equipment', 'environments'].includes(field) ? <span className="ml-1 text-rose-700">*</span> : null}</legend><div className="flex flex-wrap gap-2">{options.map(([value, optionLabel]) => <button type="button" key={value} aria-pressed={selected.includes(value)} onClick={() => toggle(field, value)} className={`rounded-lg border px-3 py-2 text-sm font-medium ${selected.includes(value) ? 'border-emerald-700 bg-emerald-700 text-white' : 'border-slate-200 bg-white text-slate-700 hover:bg-slate-50'}`}>{optionLabel}</button>)}</div></fieldset>; })}<p className="text-xs leading-5 text-slate-500">{c.onboarding.medicationPrivacy}</p><Button type="submit" disabled={saving} className="w-full bg-emerald-800 hover:bg-emerald-900">{saving ? <><LoaderCircle className="animate-spin" /> {c.common.saving}</> : <><Check /> {c.onboarding.save}</>}</Button></form></CardContent></Card>;
}
